dependencies {
    implementation project(':daepiro-common')

    // redis
    api 'org.springframework.boot:spring-boot-starter-data-redis'
}

bootJar {
    enabled = false
}
jar {
    enabled = true
}